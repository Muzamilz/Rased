{
  "name": "rased",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "shared",
    "backend/*",
    "frontend"
  ],
  "scripts": {
    "dev:api": "npm run dev --workspace=@rased/api",
    "dev:frontend": "npm run dev --workspace=@rased/frontend"
  }
}
